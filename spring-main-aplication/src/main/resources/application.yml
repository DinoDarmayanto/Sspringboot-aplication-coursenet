server:
  port: 8080

database-config:
  port: 3306
  username: test-master
  password: 12345678910
  url: jdbc:mysql://localhost:3306/test

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coursenet
    username: postgres
    password: mypassword
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob:
          non_contextual_creation: true
    ddl-auto: update
